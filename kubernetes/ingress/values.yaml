apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: cs3219-ingress
  annotations:
    kubernetes.io/ingress.global-static-ip-name: "web-static-ip"
spec:
  rules:
  - http:
      paths:
      # - path: /
      #   pathType: Prefix
      #   backend:
      #     service:
      #       name: user-service-5df6df7f4c-kwtmb-mdk67
      #       port:
      #         number: 8000
      # - path: /user
      #   pathType: Prefix
      #   backend:
      #     service:
      #       name: user-service
      #       port:
      #         number: 8000